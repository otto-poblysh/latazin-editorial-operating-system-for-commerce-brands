# Brands

This directory holds brand-specific implementations of the editorial operating system.

Keep reusable system logic in `docs/` and `templates/`. Put brand-owned inputs, calendars, latazins, and campaign materials inside each brand folder here.
